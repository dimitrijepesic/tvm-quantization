# Cross-compiled ARM artifacts (regenerable via experiments/03_cross_compile_arm.py)
# fp32 .params ~ 191 MB, int8 .params ~ 30 MB, .so ~ 200 / 33 MB
experiments/resnet50_*_arm.so
experiments/resnet50_*_arm.params
experiments/resnet50_*_arm.json

# Compiled aarch64 QEMU benchmark binaries (rebuild from qemu/*.c[pp];
# build commands in qemu/README.md and the source file headers)
qemu/microbench
qemu/resnet50_bench

# QEMU guest artifacts (see qemu/README.md)
qemu/*.img
qemu/user-data
qemu/meta-data
qemu/initramfs*
qemu/*.cpio.gz

# Python build artifacts
__pycache__/
*.py[cod]
*.egg-info/
.pkl_memoize_py3/

# LaTeX intermediates in the project root only — keep logs/*.log!
/main.aux
/main.log
/main.out
/main.toc
/main.synctex.gz
/main.fls
/main.fdb_latexmk
/*.bbl
/*.blg

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*Zone.Identifier
