# Python dependencies for TVM quantization experiments
# Install with: pip install -r requirements.txt
# Tested with Python 3.10

torch>=2.0
torchvision>=0.15
# TVM v0.15.0 predates NumPy 2.0 and is not compatible with it
numpy>=1.24,<2
